# Next Steps

- Rerun the key trigger_orchard_v3 artifacts with the split-aware recorder so the overfitting evidence is persisted directly in summary.json and metrics.json.
- Compare targeted anti-overfitting changes against the split gap, prioritizing variants that preserve test Ctd while shrinking the train-versus-val/test gap.
